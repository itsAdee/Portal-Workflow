title: Remove Payment Event
allOf:
  - $ref: "./Base-Invoice-Event.yaml"
  - type: object
    properties:
      event_data:
        $ref: "./Remove-Payment-Event-Data.yaml"
    required:
      - event_data
