title: Apply Credit Note Event
allOf:
  - $ref: "./Base-Invoice-Event.yaml"
  - type: object
    properties:
      event_data:
        $ref: "./Apply-Credit-Note-Event-Data.yaml"
    required:
      - event_data
